function loadMenuPage() {

    //Create container div that houses all content
    const container = document.createElement("div");
    container.classList.add("container", "container-menu");

    document.body.appendChild(container);
}